# Copie a "Connection string" do seu banco no painel do Neon (neon.tech)
# ou do addon de banco de dados dentro do proprio Netlify (Postgres, via Neon).
DATABASE_URL=

# Netlify Blobs nao precisa de variavel de ambiente - funciona
# automaticamente quando o site roda na Netlify (ou local via `netlify dev`).
