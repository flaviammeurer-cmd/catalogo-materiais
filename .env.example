# Preenchidos automaticamente quando voce conecta o Vercel Postgres
# e o Vercel Blob Store ao projeto (Storage > Create Database / Create Store).
# Para rodar local, copie os valores do painel da Vercel para um arquivo .env
POSTGRES_URL=
POSTGRES_URL_NON_POOLING=
BLOB_READ_WRITE_TOKEN=
